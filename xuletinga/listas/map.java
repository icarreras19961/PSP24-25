public class map {
    /*
     * Hash map es exactamente igual en metodos que el resto de listas por tanto
     * puedes ir a la api de
     * java(https://docs.oracle.com/javase/8/docs/api/index.html) o ir a cualquier
     * otro ejercicio que tengamos echo con los metodos la diferencia para utilizar
     * este respecto a los otros es que este se usa con una key y un valor es decir
     * que a la hora de buscar algo con el map solo hace falta filtrar por key y
     * luego mostrar los valore que pueden ser tanto int, string... o clases
     */
}
